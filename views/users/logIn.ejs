<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>logIn</title>
    <style>
      .container {
        display: flex;
        align-items: center;
        flex-direction: column;
        justify-content: center;
      }

      .logo {
        color: black;
        text-shadow: 0px 4px 4px rgba(0, 0, 0, 0.25);
        font-family: Inter;
        font-size: 48px;
        font-style: normal;
        font-weight: 800;
        line-height: normal;
      }
      input {
        border-radius: 5px;
        height: 50px;
        width: 293px;
      }
      .loginBut {
        margin-top: 10px;
        width: 300px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1 class="logo">Board!</h1>
      <h3><%=message%></h3>
      <form method="post" action="/user/logIn">
        <label for="userName"></label>
        <input
          name="userName"
          type="text"
          required
          placeholder="아이디"
        /><br />
        <label for="password"></label>
        <input
          name="password"
          type="password"
          required
          placeholder="비밀번호"
        /><br />
        <input type="submit" class="loginBut" value="로그인" />
      </form>
      <span>계정이 없으신가요? <a href="/user/signUp">회원가입</a></span>
    </div>
  </body>
</html>
