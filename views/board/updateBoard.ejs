<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= post.title %></title>
    <link href="/board/css/top.css" type="text/css" rel="stylesheet" />
  </head>
  <style>
    #postContainer {
      background-color: #ebebeb;
      padding-bottom: 20px;
      margin: 40px 150px 40px;
    }
    #formContainer {
      display: inline-block;
      text-align: center;
    }
    .postTitle {
      margin: 50px 0px 20px;
      width: 1100px;
      height: 70px;
      border-radius: 10px;
      font-size: 20px;
      padding-left: 40px;
      background: #d9d9d9;
    }
    textarea {
      border-radius: 10px;
      width: 1084px;
      height: 350px;
      padding: 20px 30px 0px;
      background: #d9d9d9;
    }
    #formContainer > form {
      display: inline-block;
    }
    #formContainer > form > input {
      border-radius: 40px;
      background: #d9d9d9;
      border: #d9d9d9 solid;
      width: 100px;
      height: 45px;
      margin-top: 5px;
      color: #000;
      font-family: Inter;
      font-size: 15px;
      font-weight: 300;
    }
    form {
      margin-left: 10px;
      display: inline-block;
    }
  </style>
  <body>
    <%- include('./layout/header') %>
    <div id="postContainer">
      <div id="formContainer">
        <form action="/post/<%= post.postID %>?_method=patch" method="post">
          <div class="post">
            <input
              name="title"
              class="postTitle"
              type="text"
              value="<%= post.title %>"
            />
            <textarea name="content"><%= post.content %></textarea>
          </div>
          <input type="submit" value="글 수정" />
        </form>
        <form action="/post/view/<%= post.postID %>" method="get">
          <input type="submit" value="돌아가기" />
        </form>
      </div>
    </div>
  </body>
</html>
